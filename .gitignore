.DS_Store
node_modules/
dist/
coverage/
firebase-debug.log
.firebase/
SPEC_REESCRITA_COMPLETA.md
*.local
.vercel
.env*

# Ferramentas de agente instaladas localmente (impeccable) — reinstaláveis com
# `npx impeccable install`, não fazem parte da app nem do bundle.
.github/skills/
.github/hooks/
.claude/skills/
.claude/settings.local.json
.agents/

# Saída do skill graphify (o grafo de conhecimento do repositório): 12 MB de
# HTML/JSON gerados, mais um cache, todos regeráveis a partir do próprio código
# — e com caminhos absolutos desta máquina lá dentro, que não servem a mais
# ninguém.
graphify-out/
